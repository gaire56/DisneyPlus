import React from 'react';
import styled from 'styled-components';

const Header = () => {
  return <Container>hi from header</Container>;
};

export default Header;

const Container = styled.div`
  height: 70px;
  background-color: #090b13;
`;
